"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = print;

function _objectWithoutProperties(source, excluded) { if (source == null) return {}; var target = _objectWithoutPropertiesLoose(source, excluded); var key, i; if (Object.getOwnPropertySymbols) { var sourceSymbolKeys = Object.getOwnPropertySymbols(source); for (i = 0; i < sourceSymbolKeys.length; i++) { key = sourceSymbolKeys[i]; if (excluded.indexOf(key) >= 0) continue; if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue; target[key] = source[key]; } } return target; }

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function iframePrint(props) {
  var content = props.content;
  var iframe = document.createElement('iframe');
  iframe.setAttribute('style', 'position:absolute;top:-100%;left:-100%;');
  document.body.appendChild(iframe);
  var contentWin = iframe.contentWindow;
  var contentDoc = contentWin.document;
  contentDoc.body.appendChild(content.cloneNode(true));

  contentDoc.onreadystatechange = function () {
    if (contentDoc.readyState === 'complete') {
      contentWin.print();
      iframe.parentNode.removeChild(iframe);
    }
  };
}

function windowPrint(props) {
  var content = props.content,
      title = props.title;
  var win = window.open('', '_blank');
  var doc = win.document;
  doc.title = title;
  doc.body.appendChild(content.cloneNode(true));

  doc.onreadystatechange = function () {
    if (doc.readyState === 'complete') {
      win.print();
      win.close();
    }
  };
}

function print(props) {
  var _props$newWindow = props.newWindow,
      newWindow = _props$newWindow === void 0 ? false : _props$newWindow,
      res = _objectWithoutProperties(props, ["newWindow"]);

  var printImpl = newWindow ? windowPrint : iframePrint;
  printImpl(res);
}